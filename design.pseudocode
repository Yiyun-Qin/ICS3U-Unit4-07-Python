FOR (int initial_counter = 1000; initial_counter <= 2000; initial_counter++)
    IF (initial_counter + 1) % 5 == 0 THEN
        SHOW "initial_counter\n"
    ELSE
        SHOW "initial_counter, end = """
    ENDIF
END
